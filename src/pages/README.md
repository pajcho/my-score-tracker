# src/pages

Purpose: Route-level page components (top-level views).

Notes:
- Keep page components focused on layout and data wiring.
- Defer reusable UI to `src/components/`.

Agent commands:
- `npm run dev`
- `npm run lint`
- `npm run build` (copies `dist/index.html` to `dist/404.html`)
- `npm run preview`
