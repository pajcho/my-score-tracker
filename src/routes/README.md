# src/routes

Purpose: Route configuration and route-to-page wiring.

Notes:
- Update when adding or removing pages.
- Keep route definitions and metadata centralized here.

Agent commands:
- `npm run dev`
- `npm run lint`
- `npm run build` (copies `dist/index.html` to `dist/404.html`)
- `npm run preview`
