# src/integrations

Purpose: Third-party service clients and integration wiring.

Notes:
- Keep vendor-specific code isolated in subfolders.
- Avoid mixing UI here.

Agent commands:
- `npm run dev`
- `npm run lint`
- `npm run build` (copies `dist/index.html` to `dist/404.html`)
- `npm run preview`
