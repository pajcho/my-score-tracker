# src/lib

Purpose: Shared utilities and data-access helpers (e.g., Supabase helpers, formatting).

Notes:
- Keep functions pure where possible.
- Avoid UI concerns here.

Agent commands:
- `npm run dev`
- `npm run lint`
- `npm run build` (copies `dist/index.html` to `dist/404.html`)
- `npm run preview`
